---
feature: Data retroativa da venda e exclusão de venda
module: sales (packages/shared + apps/api + apps/web)
phase: handoff
status: completed
updated: 2026-09-19
---

# Progress: Data retroativa da venda e exclusão de venda

**Status:** completed
**Current Phase:** handoff
**Current Task:** Handoff ao humano — aguardando revisão e commit

## Decisions Log

| Data | Fase | Decisão | Justificativa |
|---|---|---|---|
| 2026-09-19 | intake | Data retroativa **e** exclusão de venda no mesmo ciclo de spec | Decisão explícita do usuário. O orquestrador recomendou separar (operação destrutiva + migração no mesmo QA) e a recomendação foi recusada. Risco mitigado por milestones independentes: a QA pode reprovar uma parte sem bloquear a outra |
| 2026-09-19 | intake | Branch sai de `feature/sales-lifecycle-payment-plans`, não da `main` | CRM-12 (ADR-0023: ciclo de venda, cobranças, reserva derivada) está `[R]` e ainda não mergeado. Todo o código que esta feature modifica só existe nessa branch. **Consequência para a QA: o diff de revisão é contra `feature/sales-lifecycle-payment-plans`, não contra `main`** |
| 2026-09-19 | spec | Campo de entrada `soldOn` (dia) distinto de `soldAt` (instante) | Convenção `...On` = dia local / `...At` = instante. Evita um mesmo nome com dois tipos entre requisição e resposta |
| 2026-09-19 | spec | `soldOn` opcional; ausente ⇒ hoje | Mantém todos os chamadores atuais e a suíte existente compatíveis sem alteração |
| 2026-09-19 | spec | Data = hoje preserva `transactionNow` byte-a-byte | Isola a retroatividade em um ramo novo; o caminho quente não muda, eliminando a maior fonte de risco de regressão |
| 2026-09-19 | spec | Meio-dia local para data passada | Meia-noite local é ambígua/inexistente em virada de DST (alerta do próprio `time.ts`); datas brasileiras anteriores a 2019 têm DST |
| 2026-09-19 | spec | Cobranças seguem a data da venda (`paid_at`, `due_date`) | Decisão do usuário. Sem isso o fluxo de caixa afirmaria que o dinheiro de uma venda antiga entrou hoje — mesma classe de erro que a feature corrige |
| 2026-09-19 | spec | Exclusão permitida com cobrança paga (ao contrário do cancelamento) | É o caso de uso central: venda duplicada à vista nasce com cobrança já baixada; exigir estorno antes tornaria a limpeza inviável |
| 2026-09-19 | spec | Exclusão sem trava de prazo | Decisão do usuário. A limpeza que motivou a feature é justamente de vendas antigas |
| 2026-09-19 | spec | Correção da exibição de data (RF-07) entra no escopo | Defeito pré-existente (`toDatePart` fatia o ISO em UTC: venda das 21:00 locais exibe o dia seguinte) **dentro do raio da feature**. Entregar "a data da venda é a data certa" com esse bug vivo seria incoerente. Ampliação de escopo consciente e registrada, não silenciosa |
| 2026-09-19 | spec | Migração altera apenas CHECK constraints, sem backfill | O schema já tem todas as colunas; a nova constraint é satisfeita por transitividade em toda linha existente (prova no `plan.md`) |
| 2026-09-19 | spec | Cancelamento permanece intocado | Verbos distintos (RF-14); alterar o cancelamento ampliaria o raio de regressão sem necessidade |
| 2026-09-19 | spec | **Rodada 1 de revisão neutra: REPROVADO** — 3 achados CRÍTICOS, todos confirmados por inspeção direta do código pelo orquestrador | Achados legítimos, não aceitos por deferência: (1) o agregado do mês é keyed em `completed_at` e seria corrompido pelo próprio fluxo prescrito; (2) a migração invalida dois testes de invariante existentes enquanto o plano proibia alterá-los; (3) o teste de regressão do RF-07 não tinha onde existir na infra de teste atual |
| 2026-09-19 | spec | **RF-15**: agregado do mês recorta por `sold_at` (mantendo `status = 'completed'`) | Decisão do usuário entre três saídas apresentadas. `completed_at` é carimbado com o relógio na baixa da última parcela, então venda retroativa a prazo cairia no mês da digitação — exatamente o número que a feature existe para tornar verdadeiro. Emenda o "faturamento por conclusão" do ADR-0023 ⇒ ADR próprio. Efeito aceito: faturamento de mês passado pode subir retroativamente quando venda antiga for quitada |
| 2026-09-19 | spec | Os dois casos de invariante temporal invalidados pela migração são **reescritos como casos positivos**, nunca removidos ou skipados | `sales-tables.integration.test.ts:282` e `:623` asseveram exatamente o que o RF-02/RF-04 passam a permitir — o caso de vendas é literalmente o formato de uma venda retroativa. A premissa mudou por decisão de produto; relaxar ou skipar violaria `testing.md` |
| 2026-09-19 | spec | Correção do RF-07 vira **helper compartilhado exportado** em `lib/format.ts`, aplicado a 4 pontos de exibição | O plano original listava 2 dos 4 arquivos e pedia um teste impossível: `toDatePart` é `const` local em `.tsx`, e o Vitest coleta só `*.test.ts` em `environment: "node"` (sem jsdom). Extrair para módulo `.ts` é a única forma de o teste de regressão existir — e elimina a duplicação quádrupla |
| 2026-09-19 | spec | **RF-16**: data passada muda o padrão de entrega para "entregue" | Achado da rodada 1. Defaults atuais (`pending`/`on_delivery`) fariam cada venda histórica deixada no padrão não baixar estoque, ficar `open` para sempre, reservar produto e ficar fora do faturamento. Com ~dezenas de vendas a reinserir, o caminho de menor esforço precisa ser o correto — decisão do orquestrador, sinalizada ao usuário |
| 2026-09-19 | spec | Precondição de estoque da reentrada vira Restrição explícita + procedimento de handoff | A spec prometia estoque e histórico reais sem dizer que o `stock_qty` cadastrado precisa ser o **anterior** às vendas. Se for o atual, a reentrada bate em `InsufficientStockError` no primeiro produto |
| 2026-09-19 | spec | Devolução de estoque ignora item com `product_id` nulo | ADR-0013 item 2 deixa o item órfão quando o produto é excluído; o `cancel` já trata assim — a exclusão segue o mesmo padrão em vez de inventar comportamento |
| 2026-09-19 | spec | Correção: o banco **não** barra data futura; a guarda autoritativa é o service | Um CHECK não pode chamar `now()` — linha com `sold_at` e `updated_at` ambos no futuro passaria. O RF-02 prometia invariante que o banco não entrega |
| 2026-09-19 | spec | Correção da premissa da migração: o CHECK temporal **não está em produção** | Nasce em `0013_optimal_midnight.sql:41`, parte do CRM-12 (`[R]`, não deployado). No deploy desta feature rodam `0012`→`0013`→`0014` em sequência. DoD passa a exigir ensaio sobre restore de dump de produção, não banco novo |
| 2026-09-19 | spec | Dois ADRs, não um: exclusão de venda **e** recorte do faturamento por `sold_at` | São emendas a ADRs diferentes (0013 item 3 + invariante 5; e 0023 "faturamento por conclusão"). Nenhuma task cobria a graduação da memória — agora há task explícita com os arquivos |
| 2026-09-19 | spec | Casos de integração distribuídos por milestone, não concentrados no M7 | M2/M3/M4 ficavam sem verificação executável própria; 22 casos numa task única no fim adiava a descoberta de defeito para o pior momento |
| 2026-09-19 | spec | Bordas aceitas registradas: virada de dia com formulário aberto; dependência de relógio real no Zod | Não são defeitos a corrigir, são escolhas — registrá-las evita que a QA as trate como achado novo. Testes do schema usam `vi.useFakeTimers` |
| 2026-09-19 | spec | **Rodada 2 de revisão neutra (verificador novo): REPROVADO** — 2 CRÍTICOS + ALERTAS, todos confirmados por inspeção | Verificador independente do da rodada 1, sem acesso aos achados anteriores |
| 2026-09-19 | spec | RF-16 estendido: data passada muda **também** o padrão de condição de pagamento para "já recebido" | Trocar só a entrega resolvia metade — com `on_delivery` a cobrança nasce com `paid_at` nulo, o status fica `open` e a venda não entra em faturamento nem em lucro, que é exatamente o objetivo. Os defaults do formulário (`pending`/`on_delivery`) divergiam dos do próprio contrato (`delivered`/`received`) |
| 2026-09-19 | spec | Toda decisão de UI verificável vira **helper puro em `lib/`**; interação vira pendência de E2E declarada | O projeto não tem como testar componente React: sem jsdom, sem `@testing-library`, zero `.test.tsx`, Vitest em `node` coletando só `.ts`. Eu havia prescrito "teste do formulário" em 3 tasks — cobertura fingida, justamente o que `spec-format.md` proíbe, e contradizendo meu próprio argumento do RF-07 |
| 2026-09-19 | spec | Correção do ensaio de migração: produção está em **`0009`**, não em `0011` | A `main` (única branch que dispara deploy) não contém as migrações `0010`+. O ensaio correto é restaurar dump de produção e rodar `0010` → `0014` |
| 2026-09-19 | spec | `toDatePart` tem **7** cópias, não 4: as 3 fora do escopo de vendas (pedidos e leads) ficam como known-issue + roadmap | Mesmo defeito, outros módulos. Corrigir aqui seria ampliar escopo em silêncio; ignorar sem registrar seria esconder bug conhecido |
| 2026-09-19 | spec | Caso de concorrência **`excluir × cancelar`** acrescentado | `cancel` é a única outra operação que credita estoque — é o cenário direto do risco "crédito em dobro", classificado como alta probabilidade, e estava sem teste |
| 2026-09-19 | spec | Caso 7 desdobrado em `completed + entregue` e `open + entregue` (este via rota `deliver`) | Com o ciclo do CRM-12, "entregue mas não paga" é estado comum e tem estoque debitado; testar só o `completed` deixaria o quadrante mais frequente do dia a dia descoberto |
| 2026-09-19 | spec | Piso da data fixado em **01/01/2015** com mensagem definida | "Piso nomeado" sem valor era requisito não-verificável — o implementador inventaria a constante. 2015 barra erro de ano sem bloquear histórico legítimo (e o meio-dia do RF-03 neutraliza o DST brasileiro pré-2019) |
| 2026-09-19 | spec | Correção factual: CRM-12 **está commitado** na branch base; o que falta é merge na `main` e deploy | Eu havia escrito "não commitado", o que é falso (`ff1b673`, `d62b1bf`) |
| 2026-09-19 | spec | **Rodada 3 (verificador novo): APROVADO** — 7 alertas não-bloqueantes, todos aplicados mesmo assim | Fatos conferidos e confirmados pelo revisor: linhas, CHECKs, defaults, semântica de estoque, infra de teste, FKs, 7 cópias de `toDatePart`, produção em `0009`. Prova da migração sem backfill sustentada; reversão de estoque correta nos quatro quadrantes, inclusive sob concorrência |
| 2026-09-19 | spec | Correção factual: o formulário **não usa `zodResolver`** — `useForm` sem resolver (`sale-form.tsx:615-617`) + `createSaleSchema.safeParse` manual (`:676`) | A task citava resolver inexistente; o risco de chave stripada é real, mas vem do `safeParse`. Inclui também o ramo `soldOn` em `applyContractIssues`, sem o qual as mensagens de piso e data futura caem no genérico |
| 2026-09-19 | spec | Correção factual: revalidação do cancelamento está em `actions.ts:123-126` e **não cobre dashboard** (a constante não existe em `apps/web`) | Seguir o precedente citado deixaria o dashboard desatualizado após excluir venda entregue |
| 2026-09-19 | spec | **Lacuna objetivo↔entrega registrada**: não existe leitor de período passado | `monthSalesScope` agrega só o mês corrente e `salesListQuerySchema` não tem filtro de data. A feature torna o DADO correto; ler "lucro de março" depende do MKT-02 (Fase 4). Nomeado em Fora de Escopo e no handoff para não criar expectativa falsa |
| 2026-09-19 | spec | Leitor de período passado fica **fora desta spec**, como item CRM-14 do roadmap | Decisão do usuário entre três opções. A base correta desbloqueia a digitação do histórico imediatamente, que é o que está parado; ampliar a spec (já com 16 RFs e 24 casos de integração) atrasaria o que é urgente |
| 2026-09-19 | implement | Mensagens novas do contrato: `SOLD_ON_FUTURE_MESSAGE` ("A data da venda não pode ser no futuro") e `SOLD_ON_INVALID_MESSAGE` | Só o texto do piso estava fixado na spec; o implementador seguiu o padrão de nomenclatura e tom do arquivo |
| 2026-09-19 | implement | Piso exportado como constante `SOLD_ON_MIN_DATE = "2015-01-01"` em `index.ts` | Evita duplicar o literal no `min` do campo de data do formulário (Milestone 5) — contrato como fonte única |
| 2026-09-19 | implement | `FIRST_DUE_DATE_PAST_MESSAGE` reescrita para "O primeiro vencimento não pode ser anterior à data da venda" | A mensagem antiga ("no passado") passaria a mentir: com venda retroativa, um vencimento no passado real mas posterior à venda é válido |
| 2026-09-19 | implement | Checkpoint M1+M2 achou um **quarto** teste com premissa alterada: `sales.integration.test.ts:918` | O M1 encontrou só o de `packages/shared`. O teste na fronteira HTTP assertava a mensagem antiga por regex `/passado/i`. Comportamento (422 + código de validação) permanece idêntico — mudou só o texto esperado, agora `/anterior à data da venda/i`. Não é regressão; o plano subestimou o alcance do RF-06 |
| 2026-09-19 | implement | `deriveSaleInstant` retorna a **mesma referência** de `transactionNow` quando a data é hoje | Garante que o caminho quente é byte-a-byte idêntico ao atual, como o RF-03 exige; o teste unitário assere com `toBe` (identidade), não `toEqual` |
| 2026-09-19 | implement | Na fronteira HTTP, `soldOn` futuro retorna o código de validação do Zod, não `INVALID_SALE_DATE` | O `superRefine` barra antes do service. A guarda do service é autoritativa e **independente**, provada por teste unitário que chama `composeSaleCreation` direto com `transactionNow` injetado, sem passar pelo Zod. Mesmo padrão do teste pré-existente de `firstDueDate` |
| 2026-09-19 | implement | Task 3.4 não exigiu mudança no repository | O `create` já espalhava `...composed.sale` / `...composed.receivables` sem recalcular — a arquitetura de composição no service já estava correta, o instante novo fluiu sem ajuste |
| 2026-09-19 | implement | `buildPayload`/`parseBRLOrUndefined` extraídos para `lib/sale-form-payload.ts` | Eram funções não-exportadas dentro de `.tsx`, fora da coleta do Vitest — sem extrair, o teste exigido pelo RF-01 (que `soldOn` sobrevive ao `safeParse`) seria impossível. Mesmo movimento do helper do RF-07, e mesmo precedente (`appointment-form-payload.ts`) |
| 2026-09-19 | implement | Helper de data nomeado `formatLocalDateBr`, genérico e não específico de vendas | É reusado também nos componentes de agenda; nome sem contexto de domínio, consistente com `formatDateBr`/`formatBRL` do mesmo módulo |
| 2026-09-19 | implement | Campo "Data da venda" é a **primeira** seção do formulário | A data determina os padrões de entrega e pagamento (RF-16) exibidos abaixo — escolhê-la primeiro é a ordem natural e reduz a chance de o recálculo surpreender |
| 2026-09-19 | implement | **Comportamento em aberto**: trocar a data sobrescreve entrega/pagamento mesmo se a usuária já os tiver editado manualmente | Implementação mais simples que satisfaz o texto do RF-16 (os campos continuam editáveis). Incomoda no caso "corrigir a data depois de ajustar o pagamento". Deixado explícito para a QA avaliar — candidato a rastreamento de campo tocado, não regressão de requisito |
| 2026-09-19 | implement | Caso 9 (entregue → cancelada → excluída) teve de usar `on_delivery`, não `received` | Com `received` a cobrança nasce paga, e o `cancel` responde 409 antes de a venda chegar a "cancelada" — o teste nunca alcançava o estado que pretendia sondar. Mesmo ajuste na corrida `excluir × cancelar`, para que os dois desfechos sejam alcançáveis em vez de o cancel falhar deterministicamente. Confirma na prática por que excluir precisa ser permitido com cobrança paga (RF-10) |
| 2026-09-19 | implement | `remove` entrou como membro **obrigatório** de `SalesRepositoryPort` | Dois fakes de teste (`app.test.ts`, `leads.integration.test.ts`) ganharam stub que lança — port incompleto silencioso seria pior que o ajuste explícito |
| 2026-09-19 | implement | No aviso de exclusão, `canceled` tem **prioridade** sobre `delivered` | Venda cancelada pode ter sido entregue antes do cancelamento, e o cancelamento já devolveu aquele estoque. Se `delivered` tivesse prioridade, o aviso mentiria prometendo devolver estoque de novo — a mesma armadilha dos três estados, agora no texto da UI |
| 2026-09-19 | implement | Criada a constante `DASHBOARD_PATH = "/crm"` | Nenhuma action de vendas revalidava o dashboard até agora; excluir venda entregue devolve estoque e remove cobranças, então sem isso os números ficariam obsoletos até o próximo refresh |
| 2026-09-19 | qa | **Rodada 4 de QA neutra: APROVADO** — 1.317 testes, lint, typecheck, build e ensaio 0010→0014 verdes | A rodada anterior apontou que a cadeia legada parava em 0013 e não havia build raiz. Correções delimitadas cobrem 0014 em Postgres real e institucionalizam `bun run build` para o único workspace com artefato de build (`@clientela/web`). Nenhum achado remanescente |

## Milestones

- [x] Milestone 1: Contrato (`packages/shared`) — 252 testes verdes
- [x] Milestone 2: Migração de CHECK constraints — migração `0014`, 112 testes de integração verdes
- [x] Milestone 3: Data retroativa na API — 1290 testes verdes (+31)
- [x] Milestone 4: Exclusão de venda na API — 598 testes verdes em apps/api, 3 corridas concorrentes cobertas
- [x] Milestone 5: Web — data da venda — build do Next ok, 14 testes novos
- [x] Milestone 6: Web — exclusão de venda — 1316 testes verdes, build ok
- [x] Milestone 7: Integração e fechamento — RF-15, ADR-0024, ADR-0025, memória graduada

## Session Log

| Data | Sessão | Fase | O que foi feito | Notas |
|---|---|---|---|---|
| 2026-09-19 | 1 | intake+spec | Análise do codebase, branch `feature/retroactive-sale-date-and-deletion`, spec/research/plan/tasks autorados, item CRM-13 no roadmap | Origem: consultora recadastrou o catálogo e vai reinserir o histórico de vendas; limpeza das vendas atuais será feita por SQL direto antes da reentrada |
| 2026-09-19 | 2 | implement+qa+handoff | Implementação retomada, QA adversarial corrigida em uma rodada e aprovação final | Relatórios finais: `review.md` e `validate.md`; pendência E2E permanece declarada na spec |
